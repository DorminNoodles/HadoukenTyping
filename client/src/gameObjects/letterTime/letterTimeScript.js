// import { animFlash } from './anim/animFlash';
// import letterScoreUI from './letterScoreUI';
import GameObject from '../../core/gameObject';
// import RenderText from './renderText';
// import Render from './render';
import Script from '../../core/script';
import LetterScript from '../../letterScript';


class LetterTimeScript extends LetterScript {

	constructor(position, letter, gameObject) {
		super(position, letter, gameObject);
	}
}

export default LetterTimeScript;
